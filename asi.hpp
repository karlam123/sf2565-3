#ifndef ASI_HPP
#define ASI_HPP

// calculate the integral of f from a to b with tolerance tol using
// Adaptive Simpson Integration with recursion
double ASI(double f(double x), const double a, const double b, const double tol);

#endif // ASI_HPP
